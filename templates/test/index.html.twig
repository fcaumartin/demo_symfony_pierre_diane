{% extends 'base.html.twig' %}

{% block title %}Hello TestController!{% endblock %}

{% block body %}
<div class="container">
<div class="row">
    <div class="col-6">
        {{ form_start(form) }}

            {{ form_errors(form) }}

            {{ form_row(form.nom)}}



        {{ form_end(form) }}
        {# {{ form_end(form, {render_rest: false}) }} #}
    
    </div>
</div>
</div>
{% endblock %}
